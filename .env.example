# ============================================
# 拍照学英语 - 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# ============================================
# 必需配置
# ============================================

# OpenRouter API Key (必需)
# 获取地址: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# ============================================
# 数据库配置 (使用 Zeabur 预构建服务)
# ============================================

# PostgreSQL 连接（Zeabur 会自动注入这些变量）
# 在 Zeabur 控制台设置 DATABASE_URL，格式：
# DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
DATABASE_URL=postgresql+asyncpg://photo_english:password@your-postgres-host:5432/photo_english

# Redis 连接（Zeabur 会自动注入）
# REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}
REDIS_URL=redis://your-redis-host:6379

# ============================================
# 可选配置（使用默认值）
# ============================================

# JWT 密钥 (可选，默认: photo-english-secret-key-2024)
# JWT_SECRET=photo-english-secret-key-2024

# AI 模型配置 (可选)
# VISION_MODEL=openai/gpt-4o
# TEXT_MODEL=meta-llama/llama-3-70b-instruct

# ============================================
# 本地开发配置（不需要在 Zeabur 设置）
# ============================================

# MinIO 对象存储 (仅本地开发使用)
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin

# ============================================
# Zeabur 部署说明
# ============================================
#
# 1. 在 Zeabur 控制台为每个服务添加环境变量
# 2. 必需变量：OPENROUTER_API_KEY
# 3. Zeabur 会自动注入 PostgreSQL 和 Redis 连接信息
# 4. 其他变量使用默认值即可
#
# 详细步骤请查看：DEPLOYMENT_CHECKLIST.md 和 QUICKSTART.md
