# 拍照学英语 - UI 设计提示词（用于 Google Stitch）

你是一位专业的 UI/UX 设计师，擅长 Material Design 3 设计语言。请为"Photo English Learn（拍照学英语）"应用设计完整的用户界面。

---

## 🎨 设计要求

### 设计风格
- **Material Design 3**（Google 最新设计语言）
- 简洁、清晰、专业
- 适合教育应用的视觉风格
- 移动端优先设计

### 设计原则
1. **内容优先** - 突出学习内容，减少干扰
2. **清晰易读** - 文字大小、颜色对比度符合无障碍标准
3. **操作简单** - 大按钮、清晰的视觉层次
4. **反馈及时** - 加载状态、错误提示、成功提示

---

## 📱 页面设计需求

### 1. 登录页面

**布局要求：**
- 垂直居中布局
- 宽度限制在 400px
- 浅色背景

**必需元素：**
1. **标题区域**
   - 主标题："欢迎回来"
   - 副标题："登录拍照学英语，继续你的学习之旅"
   - 字体：Roboto Regular
   - 颜色：黑色（#000000）

2. **表单区域**
   - 用户名输入框（TextField）
   - 密码输入框（TextField with type="password"）
   - 输入框样式： outlined variant
   - 标签：中文标签

3. **按钮**
   - 登录按钮（Filled Button）
   - 颜色：主色（#1976d2 蓝色）
   - 宽度：100%
   - 高度：48px
   - 圆角：20px

4. **链接**
   - "还没有账号？立即注册"
   - 颜色：主色（#1976d2）
   - 下划线：无

**配色：**
- 背景：#fefbff（接近白色）
- 卡片背景：#ffffff（纯白）
- 主按钮：#1976d2（蓝色）
- 文字：#1b1b1f（深灰）

---

### 2. 注册页面

**布局要求：**
- 与登录页面相同布局

**必需元素：**
1. **标题区域**
   - 主标题："创建账号"
   - 副标题："加入拍照学英语，开始你的学习之旅"

2. **表单区域**
   - 用户名输入框
   - 邮箱输入框
   - 密码输入框
   - 确认密码输入框
   - 所有输入框：outlined variant

3. **按钮**
   - 注册按钮（Filled Button）
   - 与登录页面样式相同

4. **链接**
   - "已有账号？立即登录"

---

### 3. 首页

**布局要求：**
- 顶部欢迎区域
- 主要功能卡片（大）
- 次要功能卡片（小，2列网格）
- 底部导航栏（可选）

**必需元素：**

**A. 顶部欢迎区**
- 问候语："你好，{用户名} 👋"
- 副标题："今天想学什么新单词呢？"
- 左对齐
- 顶部内边距：32px

**B. 拍照识别卡片（主功能）**
- 大卡片，占据显著位置
- 渐变背景：从 #1976d2 到 #1565c0
- 图标：相机图标（大，48px）
- 标题："拍照识别"
- 描述："拍摄物体，AI 帮你学习英语单词"
- 按钮："开始拍照"（白色按钮，文字为蓝色）
- 可点击：整个卡片可点击
- 阴影：elevation 2-4

**C. 功能卡片网格（2列）**
- 左卡片：生词本
  - 图标：书本（32px）
  - 标题："生词本"
  - 描述："查看已保存的单词"
  - 颜色：主色（#1976d2）

- 右卡片：练习
  - 图标：大脑（32px）
  - 标题："练习"
  - 描述："复习学过的内容"
  - 颜色：辅助色（#9c27b0）

**D. 卡片样式**
- 圆角：12px
- 内边距：16px
- 阴影：elevation 1
- 悬停效果：elevation 3

---

### 4. 相机页面

**布局要求：**
- 顶部导航栏
- 中央相机预览区（占据大部分空间）
- 底部操作区

**必需元素：**

**A. 顶部栏**
- 返回按钮（左侧）
- 标题："拍照学英语"（居中）
- 高度：64px
- 阴影：elevation 1

**B. 相机预览区**
- 占据剩余空间
- 背景色：黑色（#000000）
- 视频预览或图片预览居中显示
- 加载指示器（如果正在识别）：居中显示

**C. 底部操作区**
- 高度：80-100px
- 阴影：elevation 3

**状态1：未拍照时**
- 左侧：相册按钮（圆形 FAB，默认颜色）
- 中央：拍照按钮（大圆形 FAB，48px，主色）
- 右侧：占位（保持对称）

**状态2：已拍照时**
- 左侧按钮："重新拍摄"（Outlined Button）
- 右侧按钮："开始识别"（Filled Button，主色）
- 两个按钮并排，等宽

---

### 5. 识别结果页面

**布局要求：**
- 滚动页面
- 顶部图片预览
- 场景描述（如果有）
- 单词列表
- 底部操作按钮

**必需元素：**

**A. 图片预览**
- 全宽显示
- 圆角：12px
- 阴影：elevation 2

**B. 场景描述（如果有）**
- 卡片样式
- 背景色：浅灰（#f5f5f5）
- 标题："场景描述"（H6，粗体）
- 内容：AI 生成的描述文字

**C. 识别到的单词**
- 标题："识别到的单词 (数量)"
- 单词列表（每个单词一个卡片）

**单词卡片样式：**
- 左侧：圆形头像（显示首字母，主色背景）
- 中央：
  - 单词（H6，粗体）
  - 中文翻译（body2，次要文字）
  - 置信度（Chip 小标签）
- 右侧：添加到生词本图标按钮

**置信度 Chip：**
- >80%：绿色（success）
- 60-80%：橙色（warning）
- <60%：灰色（default）

**D. 底部操作按钮**
- "继续拍照"（Outlined Button）
- "查看生词本"（Filled Button）
- 两个按钮并排

---

### 6. 生词本页面

**布局要求：**
- 顶部搜索栏
- 标签筛选（横向滚动）
- 单词列表
- 空状态（当没有单词时）

**必需元素：**

**A. 搜索栏**
- 全宽搜索框
- 占位符："搜索单词..."
- 前置图标：搜索图标
- 样式：outlined variant

**B. 标签筛选**
- 横向滚动的 Chip 列表
- 标签："全部"、"今天"、"本周"、"已掌握"
- 选中状态：主色背景
- 未选中：outlined 样式

**C. 单词列表**
- 卡片式列表
- 每个卡片包含：
  - 单词（H6，粗体）
  - 音标（body2，灰色）
  - 中文释义（body2）
  - 发音按钮（小图标按钮）
  - 删除按钮（小图标按钮，红色）
  - 标签（Chip）

**D. 空状态**
- 中央图标（书本，灰色，大）
- 标题："还没有生词"
- 描述："开始拍照学习吧！"
- 主按钮："去拍照"

---

### 7. 单词详情弹窗

**布局要求：**
- 全屏弹窗或底部抽屉
- 可滚动内容

**必需元素：**
1. **单词**
   - 超大字体（Display Large）
   - 粗体

2. **音标和发音**
   - 音标（inline）
   - 播放按钮（圆形 FAB，小）

3. **中文释义**
   - 列表形式
   - 每个释义一行

4. **例句**
   - 英文句子（粗体）
   - 中文翻译（次要文字）

5. **操作按钮**
   - "添加到生词本" / "从生词本移除"
   - "关闭"按钮

---

### 8. 练习页面

**布局要求：**
- 顶部进度指示
- 中央练习卡片
- 底部操作按钮

**必需元素：**

**A. 进度指示**
- 顶部显示："第 3 / 10 题"
- 进度条（Linear Progress）

**B. 练习卡片**
- 场景图片（如果有）
- 英文句子（可隐藏）
- 中文翻译（可隐藏）
- 关键词（Chip 列表，可点击）

**C. 操作按钮**
- "播放发音"
- "认识"（Filled Button，绿色）
- "不认识"（Outlined Button，红色）
- "下一个"

---

### 9. 个人中心页面

**布局要求：**
- 顶部用户信息卡片
- 设置选项列表

**必需元素：**

**A. 用户信息卡片**
- 头像（圆形，80px）
- 用户名（H5，粗体）
- 邮箱（body2，灰色）
- 编辑按钮

**B. 设置选项列表**
- 学习统计
- 每日目标设置
- 通知设置
- 暗色模式（Switch）
- 语言设置
- 关于
- 退出登录（红色文字）

每个选项：
- 图标（左侧）
- 文字
- 箭头图标（右侧，除了退出登录）

---

## 🎨 设计规范

### 颜色系统

**主色调：**
```css
--md-sys-color-primary: #1976d2;        /* 主色 - 蓝色 */
--md-sys-color-on-primary: #ffffff;      /* 主色上的文字 - 白色 */
--md-sys-color-primary-container: #d3e3fd;  /* 主色容器 */
--md-sys-color-on-primary-container: #001d36;
```

**辅助色：**
```css
--md-sys-color-secondary: #9c27b0;      /* 辅助色 - 紫色 */
--md-sys-color-on-secondary: #ffffff;
--md-sys-color-secondary-container: #f3e5f5;
```

**背景色：**
```css
--md-sys-color-background: #fefbff;      /* 页面背景 */
--md-sys-color-surface: #ffffff;         /* 卡片背景 */
--md-sys-color-surface-variant: #f0f0f0; /* 变体背景 */
```

**文字色：**
```css
--md-sys-color-on-background: #1b1b1f;   /* 页面背景上的文字 */
--md-sys-color-on-surface: #1b1b1f;      /* 表面上的文字 */
--md-sys-color-on-surface-variant: #5c5c62; /* 次要文字 */
```

**语义色：**
```css
--md-sys-color-error: #ba1a1a;           /* 错误 */
--md-sys-color-success: #2e7d32;         /* 成功 */
--md-sys-color-warning: #f57c00;         /* 警告 */
```

### 字体系统

**字体家族：**
```css
font-family: 'Roboto', 'Noto Sans SC', sans-serif;
```

**字号规范：**
- Display Large: 57px / 64px line-height
- Headline Large: 32px / 40px
- Title Large: 22px / 28px
- Body Large: 16px / 24px
- Body Medium: 14px / 20px
- Label Large: 14px / 20px

### 间距系统

使用 8px 基准：
- xs: 4px
- sm: 8px
- md: 16px
- lg: 24px
- xl: 32px
- xxl: 48px

### 圆角规范

- 按钮：20px（完全圆角）
- 卡片：12px
- 输入框：4px
- 对话框：16-24px

### 阴影规范

- 卡片：elevation 1-2
- 按钮：elevation 0-2
- 对话框：elevation 8-12
- FAB：elevation 4-6

---

## 🎯 图标要求

### 必需图标（Material Icons）

**主要功能：**
- camera_alt / photo_camera（相机）
- menu_book / auto_stories（书本）
- psychology / school（学习/大脑）
- person / account_circle（用户）

**操作：**
- search（搜索）
- add / bookmark_border / bookmark（添加/收藏）
- delete / trash（删除）
- edit（编辑）
- volume_up（音量）
- arrow_back / arrow_forward（导航）
- photo_library（相册）

**状态：**
- check_circle（成功）
- error（错误）
- info（信息）
- warning（警告）

---

## 📐 布局模板

### 卡片模板

```
┌─────────────────────────┐
│  [图标]  标题 (H6)        │
│          描述 (body2)      │
└─────────────────────────┘
内边距：16px
圆角：12px
阴影：elevation 1
```

### 按钮模板

**Filled Button（主要操作）：**
```
┌─────────────────────┐
│   按钮文字           │  文字：白色，14px，粗体
└─────────────────────┘  背景：主色（#1976d2）
高度：48px，圆角：20px
```

**Outlined Button（次要操作）：**
```
┌─────────────────────┐
│   按钮文字           │  文字：主色，14px，粗体
└─────────────────────┘  边框：1px，主色
高度：48px，圆角：20px
```

### 输入框模板

```
┌─────────────────────────┐
│ 标签                     │  标签：12px，灰色
├─────────────────────────┤  边框：1px，灰色
│ 输入内容...              │  内容：16px
└─────────────────────────┘  高度：56px
圆角：4px
```

---

## 🌈 设计建议

### 视觉层次

1. **最重要**：大标题、主按钮、核心功能
2. **重要**：卡片标题、操作图标
3. **次要**：描述文字、辅助信息
4. **最不重要**：提示文字、标签

### 对齐方式

- **文字**：左对齐（除了标题可以居中）
- **按钮**：居中或全宽
- **图标+文字**：垂直居中对齐
- **卡片内容**：左对齐

### 空白使用

- 页面边距：16-24px
- 卡片间距：16px
- 元素内边距：16px（卡片），24px（页面）
- 章节间距：32-48px

### 动画建议（最小化）

- 按钮点击：缩放 0.98（100ms）
- 卡片悬停：阴影提升（200ms）
- 页面切换：淡入淡出（200-300ms）
- 列表项：从左侧滑入（200ms，交错）

---

## 📱 移动端优化

### 触摸目标

- 最小尺寸：44x44px
- 推荐：48x48px
- 按钮：高度 48px
- 卡片点击：整个卡片可点击

### 单手操作

- 重要按钮放在屏幕下半部分
- 底部导航栏高度：64-80px
- 返回按钮在左上角（如果习惯右手）

### 拇指区域

- 主要操作：屏幕下半部分
- 次要操作：屏幕上半部分
- 底部导航：最易触达

---

## ✅ 设计交付物

请为以上 9 个页面创建完整的 UI 设计稿，包括：

1. **页面布局图**（wireframe）
2. **颜色规范**（每个元素的颜色代码）
3. **字体规范**（字号、字重、行高）
4. **间距规范**（内边距、外边距、间距）
5. **组件样式**（按钮、卡片、输入框等）

**设计输出格式：**
- Figma 文件（推荐）
- 或详细的设计规范文档

**设计要求：**
- 遵循 Material Design 3 规范
- 移动端优先（屏幕宽度：375px - 414px）
- 所有页面保持一致的视觉风格
- 提供完整的设计标注

---

## 🎨 参考资源

- Material Design 3: https://m3.material.io/
- Material Components: https://m3.material.io/components
- Material Color Tool: https://material.io/resources/color/
- Material Type Scale: https://m3.material.io/styles/typography
- Figma Material Design Kit: 官方提供

---

请按照以上要求，为"拍照学英语"应用设计完整、专业的用户界面。
