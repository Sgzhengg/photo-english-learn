# Vision Service - 应用依赖
# 注意：ML 相关依赖（ultralytics, opencv-python-headless, pillow）
# 已移至 requirements-ml.txt，由 Dockerfile 分层安装

# 数据库
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 文件处理
python-multipart==0.0.12
aiofiles==24.1.0

# HTTP 客户端
httpx==0.27.2

# Redis
redis==5.2.0

# OpenAI API（用于 GPT-4o）
openai>=1.0.0
