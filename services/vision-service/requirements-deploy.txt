# Vision Service - 轻量级依赖列表（使用 DeepInfra Gemma 3 Vision）
# 无需本地 ML 模型，直接调用云端 API（高速稳定）
#
# 优化说明：已移除 shared/utils/__init__.py 的自动导入，
# vision-service 不再需要数据库和认证依赖

# 基础框架
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# 文件处理
python-multipart==0.0.12
aiofiles==24.1.0

# HTTP 客户端（用于 DeepInfra API）
httpx==0.27.2

# OpenAI SDK（用于调用 DeepInfra Gemma 3 Vision）
openai>=1.0.0
